package io.github.hardikvaghani.flexiscale.tokens.opacity

import io.github.hardikvaghani.flexiscale.tokens.model.OpacityToken

object FlexiOpacityTokens {

    val Disabled = OpacityToken(0.38)

    val Low = OpacityToken(0.60)

    val Medium = OpacityToken(0.75)

    val High = OpacityToken(0.90)

    val Full = OpacityToken(1.00)
}